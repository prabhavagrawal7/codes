#include <bits/stdc++.h>
using namespace std;
// get minimum in data structure and max
stack<vector<int>> s; 

int func(vector<int> arrival, vector<int> departure)
{
    // train platform problem

    // sort arrival and departure
    // if arrival[i] < departure[j] then we need a platform
    // else we can use the platform
    HEAPIFY
}